# Dataset

Dataset Used: CIFAR-10

This project uses the CIFAR-10 dataset provided by TensorFlow/Keras.

The dataset is automatically downloaded when the project is executed using:

from tensorflow.keras.datasets import cifar10

Therefore, the dataset files are not included in this repository.
